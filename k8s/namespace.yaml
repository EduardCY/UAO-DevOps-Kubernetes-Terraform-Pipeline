# Namespace for the CRUD application
# Isolates resources from other applications in the cluster
apiVersion: v1
kind: Namespace
metadata:
  name: crud-app
  labels:
    name: crud-app
    environment: production
